// import { useDispatch } from "react-redux";
// import { increment } from "../config/counterSlice"

export default function EventManagement() {
    // const dispatch = useDispatch();

    return(
        <>
            {/* <button onClick={() => dispatch(increment())}>click me</button> */}
            hello
        </>
    )
}